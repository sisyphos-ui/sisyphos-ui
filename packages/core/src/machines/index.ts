/**
 * Framework-agnostic state controllers.
 *
 * Each export here is pure JS/TS — no React, Vue, or Angular imports.
 * Framework bindings (in @sisyphos-ui/react, @sisyphos-ui/vue,
 * @sisyphos-ui/angular) consume these controllers and adapt them to
 * each framework's reactivity model.
 */
export * from "./checkbox";
